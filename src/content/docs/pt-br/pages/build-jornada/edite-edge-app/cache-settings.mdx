---
title: Configure uma política de cache via API
description: >-
  Ative o Application Accelerator, crie uma cache setting com TTL e segmentação
  de cache key personalizados e aplique-a com uma regra, tudo pela Azion API.
meta_tags: 'build, cache, ttl, browser, edge, object, api'
namespace: docs_guides_build_tune_cache_settings
permalink: /documentacao/build/cache/guides/ajustar-cache-settings/
menu_namespace: cacheMenu
difficulty: Intermediate
---

Você pode configurar as políticas de cache de uma aplicação pela Azion API: valores de expiração para o cache do edge e para o navegador, otimização de arquivos grandes e segmentação de cache key por método HTTP, query string, cookie ou device group.

Para fazer o mesmo pelo Azion Console, consulte [Configure uma política de cache](/pt-br/documentacao/build/cache/guides/cache-settings/).

:::caution[atenção]
Algumas funcionalidades de cache exigem que o módulo [Application Accelerator](/pt-br/documentacao/build/application-accelerator/) esteja ativo. Se o **Application Accelerator** estiver ativado, a transferência de dados pode gerar custos relacionados ao uso. Consulte a [página de preços](/pt-br/documentacao/platform/pricing/) para mais informações.
:::

---

## Pré-requisitos

- Um [personal token](/pt-br/documentacao/account/guides/personal-tokens/).
- Uma aplicação na Azion Platform e o [ID dela](/pt-br/documentacao/build/applications/guides/definir-configuracoes-principais/).

Quando você [cria uma aplicação](/pt-br/documentacao/get-started/start-with-a-template/), uma cache setting é criada e ativada por padrão. Este guia cria e ativa uma nova.

---

## Ative o Application Accelerator

O módulo libera os campos avançados de cache key usados adiante neste guia.

Para ativar o módulo:

1. Execute a seguinte requisição `PATCH` em seu terminal, substituindo `[TOKEN VALUE]` pelo seu personal token e a variável `<application_id>` pelo ID da sua aplicação:

```bash
curl --location --request PATCH 'https://api.azionapi.net/v4/edge_application/applications/<application_id>' \
--header 'Accept: application/json;' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token [TOKEN VALUE]' \
--data '{
    "application_acceleration": true
}'
```

2. Você receberá uma resposta com o valor atualizado.

A aplicação aceita os campos avançados de cache key.

---

## Obtenha o ID de um device group

A variação de cache por device group precisa do ID do grupo. Pule esta seção quando sua política não variar por dispositivo.

Para obter o ID:

1. Execute a seguinte requisição `GET`, substituindo `[TOKEN VALUE]` pelo seu personal token:

```bash
curl --location 'https://api.azionapi.net/v4/edge_application/applications/<application_id>/device_groups' \
--header 'Accept: application/json;' \
--header 'Authorization: Token [TOKEN VALUE]'
```

2. Copie o ID recebido na resposta.

Você tem agora o `<device_group_id>` usado na próxima seção.

---

## Crie a cache setting

O corpo da requisição define o TTL do navegador, o TTL do edge, a otimização de arquivos grandes e todas as regras de variação de cache em uma única chamada.

Para criar a configuração:

1. Execute a seguinte requisição `POST`, substituindo `[TOKEN VALUE]` pelo seu personal token, `<application_id>` pelo ID da sua aplicação e `<device_group_id>` pelo ID da seção anterior, se necessário:

```bash
curl --location 'https://api.azionapi.net/v4/edge_application/applications/<application_id>/cache_settings' \
--header 'Accept: application/json;' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token [TOKEN VALUE]' \
--data '{
  "name": "/cache O60 O13660 Wcity Wcookie_name Wdg POST OPTIONS SLICE",
  "browser_cache": {
    "behavior": "override",
    "max_age": 60
  },
  "modules": {
    "edge_cache": {
      "behavior": "override",
      "max_age": 13660,
      "stale_cache": {
        "enabled": false
      },
      "large_file_cache": {
        "enabled": true,
        "offset": 1024
      }
    },
    "application_accelerator": {
      "cache_vary_by_method": [
        "post",
        "options"
      ],
      "cache_vary_by_querystring": {
        "behavior": "whitelist",
        "fields": [
          "city"
        ],
        "sort_enabled": false
      },
      "cache_vary_by_cookies": {
        "behavior": "whitelist",
        "cookie_names": [
          "cookie_name"
        ]
      },
      "cache_vary_by_devices": {
        "behavior": "whitelist",
        "device_group": [
          <device_group_id>
        ]
      }
    }
  }
}'
```

2. Você receberá uma resposta com `"status": "pending"` e um objeto `data` contendo o `id` da nova cache setting.

Copie esse `<cache_settings_id>`. A regra da próxima seção precisa dele.

---

## Campos da requisição da cache setting

Cada chave abaixo pertence ao corpo da requisição da seção anterior.

| Chave | Descrição |
| --- | --- |
| `name` | Define uma string como o nome da configuração de cache. |
| `browser_cache.behavior` | Define o comportamento de cache para navegadores. `"honor"` respeita os cabeçalhos de TTL enviados pela origem; `"override"` permite definir um valor de TTL personalizado usando `max_age`. |
| `browser_cache.max_age` | TTL máximo (em segundos) para cache do navegador quando `behavior` está configurado como `"override"`. |
| `modules.edge_cache.behavior` | Define o comportamento do cache do edge. `"honor"` usa os cabeçalhos de TTL da origem; `"override"` utiliza o campo `max_age` em vez disso. |
| `modules.edge_cache.max_age` | TTL (em segundos) para o cache do edge ao substituir os cabeçalhos de TTL da origem. |
| `modules.edge_cache.stale_cache.enabled` | Habilita o [stale cache](/pt-br/documentacao/build/cache/reference/cache-settings/#stale-cache) para servir conteúdo expirado temporariamente enquanto busca conteúdo atualizado na origem. |
| `modules.edge_cache.large_file_cache.enabled` | Habilita a [Otimização de arquivos grandes](/pt-br/documentacao/build/cache/reference/cache-settings/#large-file-optimization) ao armazenar arquivos grandes em fragmentos. |
| `modules.edge_cache.large_file_cache.offset` | Define o tamanho do fragmento (em kilobytes) para o cache de arquivos grandes. O valor padrão é `1024`. |
| `modules.tiered_cache.topology` | Define a topologia usada no Cache: `"near-edge"` ou `"near-origin"` (quando disponível). |
| `modules.application_accelerator.cache_vary_by_method` | Lista de métodos HTTP (por exemplo, `["post", "options"]`) que devem variar a cache key. |
| `modules.application_accelerator.cache_vary_by_querystring.behavior` | Define como as query strings afetam a variação do cache. As opções são `"ignore"`, `"whitelist"` ou `"blacklist"`. |
| `modules.application_accelerator.cache_vary_by_querystring.fields` | Lista de campos de query string permitidos ou bloqueados ao variar o cache, dependendo do comportamento definido. |
| `modules.application_accelerator.cache_vary_by_querystring.sort_enabled` | Habilita a ordenação dos parâmetros de query string antes de calcular as cache keys. |
| `modules.application_accelerator.cache_vary_by_cookies.behavior` | Define como os cookies afetam a variação do cache. As opções são `"ignore"`, `"whitelist"` ou `"blacklist"`. |
| `modules.application_accelerator.cache_vary_by_cookies.cookie_names` | Lista dos nomes de cookies usados para variar o cache. |
| `modules.application_accelerator.cache_vary_by_devices.behavior` | Define como os grupos de dispositivos afetam a variação do cache. As opções são `"ignore"`, `"whitelist"` ou `"blacklist"`. |
| `modules.application_accelerator.cache_vary_by_devices.device_group` | Lista dos IDs de grupos de dispositivos usados para variar o conteúdo em cache de acordo com o dispositivo do usuário. |

---

## Aplique a configuração com uma regra

Uma cache setting permanece inativa até que uma regra a referencie. Esta regra aplica a configuração a todas as requisições para o caminho `/cache`.

Para criar a regra:

1. Execute a seguinte requisição `POST`, substituindo `[TOKEN VALUE]` pelo seu personal token, `<application_id>` pelo ID da sua aplicação e `<cache_setting_id>` pelo ID da cache setting da seção anterior:

```bash
curl --location 'https://api.azionapi.net/v4/edge_application/applications/<application_id>/request_rules' \
--header 'Accept: application/json;' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token [TOKEN VALUE]' \
--data '{
    "name": "Set cache setting /cache",
    "behaviors": [
        {
            "name": "set_cache_policy",
            "target": "<cache_setting_id>"
        }
    ],
    "criteria": [
        [
            {
                "variable": "${uri}",
                "operator": "is_equal",
                "conditional": "if",
                "argument": "/cache"
            }
        ]
    ]
}'
```

2. Você receberá uma resposta com os dados atualizados.

A regra aplica a cache setting ao caminho `/cache`. As alterações podem levar alguns minutos para propagar.

:::tip
Consulte a [documentação da Azion API](https://api.azion.com/) e a [especificação OpenAPI](https://github.com/aziontech/azionapi-openapi/) para saber mais sobre todas as funcionalidades disponíveis via API.
:::

---

## Próximos passos

- [Verifique indicadores de cache no navegador](/pt-br/documentacao/build/cache/guides/verificar-tempo-de-cache-da-pagina/) - confirme como seu conteúdo está sendo armazenado em cache.
- [Cache Settings](/pt-br/documentacao/build/cache/reference/cache-settings/) - todos os campos que uma cache setting aceita.
- [Cache keys](/pt-br/documentacao/build/cache/reference/cache-keys/) - como as regras de variação acima alteram a chave armazenada.
