---
title: Criar um bucket do Object Storage
description: >-
  Crie um bucket no Object Storage e altere as permissões pelo Azion Console,
  pela Azion CLI ou pela API.
meta_tags: 'Object Storage, bucket, create bucket, bucket permissions, edge storage'
namespace: docs_store_journey_storage_create_bucket
permalink: /documentacao/produtos/store/storage/criar-bucket/
---

import Tabs from '~/components/tabs/Tabs'
import Code from '~/components/Code/Code.astro'

Você pode criar um bucket do [Object Storage](/pt-br/documentacao/produtos/store/object-storage/) e alterar as permissões pelo Azion Console, pela CLI ou pela API.

## Pré-requisitos

Para usar os procedimentos da CLI, você precisa da Azion CLI instalada e de um personal token configurado.

---

## Criar um bucket

:::note[nota]
Os nomes de bucket são exclusivos entre todas as contas da Azion. Um nome deve ter de 6 a 63 caracteres e não pode começar com `azion`. Um nome pode ter caracteres alfanuméricos e hifens.
:::

<Tabs client:visible>
    <Fragment slot="tab.console">Console</Fragment>
    <Fragment slot="tab.cli">CLI</Fragment>
    <Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">

Para criar o bucket pelo Azion Console:

1. Acesse o [Azion Console](https://console.azion.com/) > **Object Storage**.
2. Selecione **+ Bucket**.
3. Digite um **Bucket Name** de 6 a 63 caracteres.
4. Defina **Workloads Access** como _Read Only_, _Read-Write_ ou _Restricted_.
5. Selecione **Save**.

O bucket aparece na lista de buckets.

</Fragment>

<Fragment slot="panel.cli">

Para criar o bucket com a Azion CLI:

<Code lang="bash" code={`azion create edge-storage bucket --name '<your-bucket-name>' --edge-access 'read_only'`} />

A flag `--edge-access` aceita `read_only`, `read_write` ou `restricted`.

Você agora tem um bucket com o nível de acesso da flag `--edge-access`.

</Fragment>

<Fragment slot="panel.api">

Para criar o bucket, envie uma requisição `POST` para o endpoint de buckets:

<Code lang="bash" code={`curl --location 'https://api.azion.com/v4/storage/buckets' \\
--header 'Accept: application/json' --header 'Content-Type: application/json' \\
--header 'Authorization: Token [TOKEN VALUE]' \\
--data '{"name": "<your-bucket-name>", "edge_access": "read_only"}'`} />

A resposta retorna `"state": "executed"` com o nome do bucket e o nível de acesso:

```json
{
  "state": "executed",
  "data": {
    "name": "...",
    "edge_access": "read_only"
  }
}
```

</Fragment>

</Tabs>

---

## Alterar as permissões de um bucket

<Tabs client:visible>
    <Fragment slot="tab.console">Console</Fragment>
    <Fragment slot="tab.cli">CLI</Fragment>
    <Fragment slot="tab.api">API</Fragment>

<Fragment slot="panel.console">

Para alterar as permissões pelo Azion Console:

1. Acesse o [Azion Console](https://console.azion.com/) > **Object Storage**.
2. Na lista de buckets, selecione o nome do bucket.
3. Altere o nível de **Workloads Access**.
4. Selecione **Save**.

O bucket tem o novo nível de acesso.

</Fragment>

<Fragment slot="panel.cli">

Para alterar as permissões com a Azion CLI, use o comando `azion update edge-storage bucket`. [GAP: flags e sintaxe completa do comando `azion update edge-storage bucket`]

</Fragment>

<Fragment slot="panel.api">

Para alterar as permissões, envie uma requisição `PATCH` para `https://api.azion.com/v4/storage/buckets/<your-bucket-name>` com este corpo:

<Code lang="json" code={`{"edge_access": "read_write"}`} />

[GAP: headers da requisição `PATCH`]

O bucket tem o novo valor de `edge_access`.

</Fragment>

</Tabs>

Para mais informações, consulte [Atualizar um bucket do Object Storage](/pt-br/documentacao/produtos/store/storage/atualizar-buckets/).

---

## Próximos passos

- [GAP: link em português para "Upload an object to a bucket"] - Adicione objetos ao bucket criado.
- [GAP: link em português para "Use a bucket as origin"] - Sirva conteúdo a partir do bucket.
