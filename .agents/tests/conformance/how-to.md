# Conformance: how-to (EN + PT-BR pair)

Files reviewed: `out/how-to.mdx`, `out/how-to.pt-br.mdx`. Brief: `.agents/tests/briefs/how-to.md`. Kind: How-to (procedural, 20-word cap).

## Checklist

- **Opening move** — PASS. EN line 14 is one scope sentence on the kind's formula: what the page lets the reader do and from where ("You can create an Object Storage bucket and change its permissions from Azion Console, the Azion CLI, or the API."). No "In this guide". PT line 14 mirrors it. Decided by `content-types.md` (How-to).
- **Section order** — PASS. `## Prerequisites` (line 16) → one `##` per task, imperative verb phrases (`## Create a bucket` line 22, `## Change the permissions of a bucket` line 86) → `## Next steps` (line 130), with `---` between major sections (lines 20, 84, 128) and none directly after the frontmatter. Same order in PT with the fixed Portuguese section names (`Pré-requisitos`, `Próximos passos`). Prerequisites as a prose sentence (line 18) rather than a bullet list is correct: the brief supplies a single prerequisite, and `style-guide.md` (Lists) mandates "a list of one is a sentence — including a single prerequisite". Decided by `content-types.md`, `style-guide.md`, `bilingual.md`.
- **Closing** — PASS. EN `## Next steps` holds two bulleted links shaped `[Title](/path/) - reason` (lines 132–133), both permalinks given in the brief, language-prefixed with trailing slash. PT lines 132–133 use `[GAP: ...]` for the two links the brief does not supply in Portuguese — the brief orders exactly this ("PT links not listed here: [GAP] rather than guessed"), so it is correct behavior. Decided by `content-types.md` (Closings matrix), brief line 57.
- **Heading grammar** — PASS. All headings sentence case; task headings imperative (`Create a bucket`, `Change the permissions of a bucket`; PT `Criar um bucket`, `Alterar as permissões de um bucket`); non-task headings noun phrases; no gerund leads, no questions, no skipped levels (all H2), no body H1 (grep verified). Decided by `style-guide.md` (Headings and titles).
- **Step grammar** — PASS. Canonical Console first step verbatim in both Console procedures (EN lines 37, 97: `Access [Azion Console](https://console.azion.com/) > **Object Storage**.`; PT lines 37, 97 the translated canonical). One action per step throughout. Location before action (EN line 98: "In the bucket list, select the name of the bucket."). UI verbs only — select, enter, set, change; zero click/hit/tap/enable/disable (grep verified). Bold labels (**+ Bucket**, **Bucket Name**, **Workloads Access**, **Save**), italic values (_Read Only_, _Read-Write_, _Restricted_). Every completed procedure ends with an outcome sentence (EN lines 43, 55, 68 + response, 102, 120); "The bucket has the new access level." is the canonical entailed-state-change example from `procedures.md` itself. Colon lead-in before every procedure and code block (EN lines 35, 49, 61, 68, 95, 114). The CLI change panel (EN line 108) holds no executable procedure — only the command name the brief gives plus a `[GAP]` for its syntax — so no outcome sentence dangles there; see Facts. Decided by `procedures.md`.
- **Frontmatter** — PASS. Five fields in both files. EN description 107 chars, PT 103 (measured; 50–160 range), imperative openers ("Create...", "Crie..."), no banned opener, neither restates its title word for word. Permalinks lowercase ASCII, trailing slash, no language prefix, exactly as the brief gives them (EN line 8, PT line 8). Namespace `docs_store_journey_storage_create_bucket` snake_case and character-identical across the pair (both line 7). PT `meta_tags` left in English per convention. Decided by `style-guide.md` (Frontmatter), `bilingual.md`.
- **Voice sweep** — PASS. Grep sweeps on both files: zero contractions; zero "we"; zero filler or marketing terms (leverage, seamless, dive into, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., utilize, in order to, note that, various, empower, plethora — all absent); zero "click"/"Learn more"/"read more"/bare URLs; link sentences use only the standard forms ("For more information, refer to ..." EN line 126; "Para mais informações, consulte ..." PT line 126). No time-bound phrasing — "the new access level" (lines 102, 120) is the outcome-sentence form `procedures.md` prescribes, not a timeline claim. Decided by `style-guide.md`, `procedures.md`.
- **Components** — PASS. Only live components: `Tabs`, `Code`, `Fragment`, `:::note` aside. Import paths match `components.md` exactly and sit directly after the frontmatter. `client:visible` on both `<Tabs>` in both files (grep: 2 per file). Every `tab.x` has its `panel.x` (grep verified, both blocks, both files). Blank lines around markdown inside every panel `<Fragment>`. Aside type valid; PT localizes the label `:::note[nota]` (line 24). `<Code>` for copyable input (CLI command, curl, PATCH body), fence for the JSON response the reader reads; `json`/`bash` tags in the allowed set; line continuations doubled (`\\`) inside the `<Code>` template literal; no bare `<br>`. Decided by `components.md`.
- **Sentence caps** — PASS. Five longest sentences spot-checked: EN opening (line 14) = 20 words, exactly at the procedural cap; note sentences (line 25) = 14/8/8; prerequisites (line 18) = 16; CLI-change sentence (line 108) ≈ 15; API-change lead-in (line 114) = 13. PT opening (line 14) = 20; longest note sentence = 15. Nothing over 20. Decided by `simplified-technical-english.md`.
- **Size** — PASS. Measured on the body: EN 3,554 chars, PT 3,562 (target 8,000, cap 16,000). Largest section: EN `Create a bucket` 1,711 chars, PT 1,732 (target 2,000, cap 4,000; `<Tabs>` sections exempt to 4,000 regardless). Decided by `page-size.md`.
- **Facts** — PASS. Every value traces to the brief: Console fields and steps, naming rules (6–63 chars, exclusive across accounts, no `azion` prefix, alphanumeric + hyphen), CLI command and `--edge-access` values, update command name, curl request, response shape, PATCH endpoint and body, all six given permalinks. Four `[GAP]` markers (EN lines 108, 118; PT lines 108, 118, plus PT Next-steps links 132–133) each cover a fact the brief withholds — CLI update syntax, PATCH headers, PT link permalinks — which is the mandated behavior, so PASS. The three outcome sentences without a brief-quoted result ("You now have a bucket...", "The bucket has the new access level.", "The bucket has the new `edge_access` value.") state entailed state changes and invent no interface output, which `procedures.md` explicitly permits and exemplifies. Nothing on either page is invented. Decided by the brief, `procedures.md`, `style-guide.md`.

## Verdict

**CONFORMANT.** All eleven checklist items pass on both files of the pair. No manual edits required.

Notes (no finding rises to a failure):

- PT line 14 says "pela CLI" where the rest of the pair says "Azion CLI"; harmless, but the full name would be more consistent.
- The CLI change panel is a stub by construction of the brief (command name + `[GAP]`); when the syntax fact is supplied, it should take the single-command form with a colon lead-in, a `<Code>` block, and an outcome sentence per `procedures.md`.
