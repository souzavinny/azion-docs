# Conformance review: tutorial — Build a user list API with Functions and SQL Database

Page: `out/tutorial.mdx` · Brief: `.agents/tests/briefs/tutorial.md` · Kind: **Tutorial** (procedural, 20-word cap)

## Checklist

- **Opening move** — PASS. Line 13 opens `In this tutorial, you will build a user list API with **Functions** and **SQL Database**.`, verbatim on the formula in `content-types.md` (Tutorial), followed by the mandated one-sentence sub-goal enumeration ("You will create a database, seed a `users` table, create a function, and route requests to it.").
- **Section order** — PASS. `## Prerequisites` first (line 15), then `## 1.` through `## 7.` numbered imperative stage headings in build order (lines 22, 40, 55, 84, 109, 123, 139); the final stage verifies (`## 7. Verify the API`). Matches the Tutorial skeleton in `content-types.md`. Three `---` separators (lines 20, 82, 137), none immediately after the frontmatter (`style-guide.md`).
- **Closing** — PASS. `## Next steps` (line 147) with three bulleted links shaped `[Title](/path/) - reason` (lines 149-151), per `content-types.md` closing matrix. All links absolute, `/en`-prefixed, trailing slash (`style-guide.md`).
- **Heading grammar** — PASS. All headings sentence case, imperative for stages, noun phrase for `## Prerequisites` and `## Next steps`; no gerund leads, no questions, no body H1, no skipped levels (all body headings are H2). Decided by `style-guide.md`.
- **Step grammar** — PASS. Canonical Console first step in all three Console procedures (lines 88, 113, 127): `Access [Azion Console](https://console.azion.com/) > ...` per `procedures.md`. One action per step; the two compound steps (line 118 "Enter a name ... and select the function", line 132 "select _Run Function_ and select the function") match the one-gesture allowance and the brief's own grouping. Location before action (lines 128, 131-132). No `(Optional)` steps used. Bold labels, italic values (`_is equal_`, `_Run Function_`, line 131-132). Outcome sentence after every procedure (lines 31, 38, 49, 80, 107, 121, 135, 145). Colon lead-in before every procedure and code block (lines 24, 33, 42, 57, 64, 86, 91, 111, 125, 141). Single-command API procedures correctly unnumbered with method-and-endpoint lead-ins. Timing warning on verification (line 145) matches `procedures.md` verbatim.
- **Frontmatter** — PASS. Five fields present (lines 2-8). Description 110 characters, imperative opener ("Create"), no banned opener, does not restate the title word for word (`style-guide.md` description contract). Permalink `/documentation/products/guides/build-a-user-list-api/` — lowercase ASCII, trailing slash, no language prefix. Namespace `docs_guides_build_user_list_api` — snake_case. Both match the brief exactly.
- **Voice sweep** — PASS. Grep swept: zero contractions (line 5 "application's" is a possessive), zero "we", zero filler or marketing terms from `style-guide.md` (leverage, seamless, dive into, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., note that, in order to, utilize — all absent), zero "click", zero "Learn more", zero forbidden expressions from `terminology.md`, no time-bound phrasing. Link phrasing uses only the standard forms (line 107 "For more information, refer to ...").
- **Components** — PASS. Only live components: `Code` imported correctly after the frontmatter (line 11, `components.md`); `<Code>` for copyable input, a fence for the read-only response shape (lines 66-78); one valid `:::note` aside (lines 51-53). No `<Tabs>` — correct, tutorials forbid them (`content-types.md`). Template-literal escaping inside `<Code>` is correct: doubled backslashes for line continuations, `'\\''` for embedded shell quotes, no unescaped backtick or `${` (the `${uri}` on line 131 is prose inline code, safe).
- **Sentence caps** — PASS. The five longest prose sentences measure 17, 16, 16, 16, 15 words — all inside the 20-word procedural budget from `simplified-technical-english.md`. No compound tenses; "Activating" (line 114) is a gerund noun, allowed; no trailing participle clauses; no noun cluster over three units.
- **Size** — PASS. Body 5,434 characters (target 8,000); largest `##` section 973 characters (target 2,000), per `page-size.md`.
- **Facts** — PASS. Every Azion-specific value traces to the brief: endpoints, headers `Authorization: Token [TOKEN VALUE]`, body JSON, response strings (`pending`, `creating`, `created`, `executed`, response shape), function code (verbatim core plus the given `addEventListener` line), Console labels and navigation, `${uri}` / _is equal_ / `/users` criteria, `xxxxxxxxxx.map.azionedge.net`, `<your-azion-domain>`, HTTP `200`, the 100-column and 30-second limits, all four link permalinks, and the propagation warning. Two `[GAP]` markers stand exactly where the brief lacks a fact — line 42 (how to obtain the database ID) and line 101 (the `handle_request` body) — which is the mandated behavior, so PASS.

## Judgment calls, labeled

- **`Content-Type: application/json`** (lines 28, 46, 61) does not appear in the brief as a header. Ruled derived encoding, not an invented value: the brief gives each request's body as JSON, and the header is the HTTP declaration of that given fact, like the curl syntax and quote escaping themselves. It cannot be wrong independently of the brief's own "body is JSON" claim, so it fails the invented-specific test in `style-guide.md` ("came from pattern-matching a similar product"). Same ruling for the polling `GET` command (lines 35-36), assembled entirely from given parts (same endpoint, same auth header).
- **"Activate the **Functions** module"** (line 114). "Activate" is outside the UI-verb table in `procedures.md`, but the brief transcribes the Console fact with that verb, `procedures.md` requires the interface's own words, and `simplified-technical-english.md` admits "the verbs the Console uses" as Technical Verbs. Not a finding; changing it would alter a given fact.

## Verdict

**CONFORMANT.** All eleven checklist items pass. Both `[GAP]` markers are correct behavior, not defects. Adversarial passes attempted: brief-external value hunt (settled by the two judgment calls above), step-compounding hunt (both compound steps sit inside the one-gesture allowance), and the literal voice sweeps — all held.
