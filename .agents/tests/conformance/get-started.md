# Conformance: get-started — `out/get-started.mdx`

Kind: **Get started** (base form Tutorial, procedural register, 20-word cap). Checklist from `.agents/tests/README.md`; deciding rules cited per check.

## Checklist

- **Opening move — PASS.** Line 13 opens `This guide instructs you through the deployment of your first application from a template.` — the verbatim formula `This guide instructs you through <outcome>.` with the outcome framed as a first ("your first application"), followed by a short bullet list (lines 15–17). The interstitial `The guide has three stages:` is a colon lead-in for the list, permitted by `style-guide.md` (lists) and not a formula deviation. Rule: `content-types.md` § Get started.
- **Section order — PASS.** `## Prerequisites` as an H2 (line 21, bulleted, each item a link or noun phrase per the skeleton), then numbered stages `## 1.` (line 28), `## 2.` (line 38), `## 3.` (line 48), each an imperative verb phrase, the final stage verifying. Rule: `content-types.md` § Get started.
- **Closing — PASS.** `## Next steps` (line 58) with four bulleted links, each shaped `[Title](/path/) - reason` (lines 60–63), matching the closing matrix. Note, not a failure: the reason on line 61 ("The full procedure to deploy a template.") is a noun phrase while the other three lead with verbs — a minor parallelism wobble under `style-guide.md` § Lists, inside the required shape. Rule: `content-types.md` § Closings.
- **Heading grammar — PASS.** All headings sentence case; task stages imperative (lines 28, 38, 48); `Prerequisites` and `Next steps` noun phrases; no gerund leads, no questions, no skipped levels (only `##` used), no body H1. Rule: `style-guide.md` § Headings and titles.
- **Step grammar — PASS.** Canonical Console first step on line 32: `Access [Azion Console](https://console.azion.com/) > **+ Create**.` — the canonical string with the navigation chain extended to the UI element per `procedures.md` § The first step. One action per step throughout; line 34 ("Browse the templates and choose one") is one gesture, supplied verbatim by the brief. Location before action (line 33, line 42); condition before action (line 43). UI verbs conform: select, enter, access; no click/hit/tap/enable/disable (grep-verified); the brief's "bottom-right corner" was correctly dropped per the no-directional-language rule. Bold UI labels (**+ Create**, **Templates**, **Connect with GitHub**, **Deploy**); no italic values needed; no `(Optional)` steps needed. Colon lead-ins before both numbered procedures (lines 30, 40). Stage 3 is a single-action procedure, correctly not numbered (`procedures.md`: numbering starts at two actions) with purpose-before-action phrasing (line 52). Outcome sentence after every procedure: line 36, line 46, line 54 — plus the propagation warning `procedures.md` § After the procedure requires, on line 54. Rules: `procedures.md`, `style-guide.md` § UI language.
- **Frontmatter — PASS.** Exactly five fields (lines 2–10). Description is 111 characters (inside 50–160, measured), opens with the imperative "Deploy", no banned opener, does not restate the title. Permalink `/documentation/products/build/applications/get-started/` — lowercase ASCII, trailing slash, no language prefix, matches the brief. Namespace `docs_build_applications_get_started` — snake_case, matches the brief. Title matches the kind formula `Get started with <Product>`. Rule: `style-guide.md` § Frontmatter; `content-types.md` § Get started.
- **Voice sweep — PASS.** Grep-verified zero contractions; zero "we"; zero hits for leverage, seamless, dive into, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., utilize, in order to, note that, various, and the marketing set; zero "click"; zero "Learn more"; no time-bound phrasing. Closing links carry reasons; body links name their destinations. Rule: `style-guide.md` § Voice, § Word choice, § Links.
- **Components — PASS.** No components used; none needed. No `<Tabs>` — correct, the kind forbids them ("One linear path"). No asides, so no invalid types. No copyable input exists on this Console-only page, so `<Code>` is not required. Links are plain markdown, absolute, `/en`-prefixed, trailing slash. Rules: `content-types.md` § Get started; `components.md`.
- **Sentence caps — PASS.** Procedural budget is 20 words. Measured the five longest prose sentences: the longest is the line-60 link reason at 16 words (the 24-token line count includes the link title, which is not the sentence); all steps and outcome sentences are 15 words or fewer. No compound tenses, no `-ing` as a verb, no trailing participle clauses, no dropped articles. Rule: `simplified-technical-english.md`.
- **Size — PASS.** Body 2,108 characters (target 8,000, cap 16,000); largest section (`Next steps`) 599 characters (target 2,000, cap 4,000). Measured. Rule: `page-size.md`.
- **Facts — PASS.** Every value traced to `briefs/get-started.md`: the prerequisites and the Console URL; the **+ Create** → **Templates** → browse/choose → configure → **Connect with GitHub** → **Deploy** flow; the deployment-logs window and the completion display (application name, logs, **Azion domain**); the domain format `xxxxxxxxxx.map.azionedge.net/` including its trailing slash; the cannot-modify constraint; the verify step, the few-minutes activation delay, and the wait-and-retry guidance; all four Next-steps links and permalinks (with the `/en` prefix added per the link rule, not invented). No `[GAP]` markers appear and none are needed — the brief covered every fact the skeleton demands. Rule: `tests/README.md` § Brief rules.

## Verdict

**CONFORMANT.** All eleven checks pass. One cosmetic observation (non-parallel reason on line 61) does not breach any checklist item.
