# Conformance: overview (Data Stream)

Page: `out/overview.mdx` · Brief: `.agents/tests/briefs/overview.md` · Kind: Overview (base form Reference, descriptive register, 25-word cap).

## Checklist

- **Opening move** — PASS. Line 13 follows the formula `**<Product>** is a <capability definition>.` verbatim in its fixed part, followed by the benefit list (lines 15–19, behavior terms, parallel verbs) and the CTA block (line 21 primary, line 23 secondary as `[GAP]` — the brief supplies no reference-page permalink, so the GAP is correct). Decided by `content-types.md` (Overview). Two notes, neither a failure: the capability definition runs 19 words against the formula's "roughly 10 to 15" (the full sentence is 22 words, inside the 25-word descriptive cap, and `simplified-technical-english.md` marks the caps as targets, not gates); the ASCII-encoding sentence on line 13 sits inside the definition block, which the Reference base form allows ("one to three definitional sentences").
- **Section order** — PASS. Definition block with CTAs (13–23); one noun-phrase `##` per capability: Data sources (27), Templates (36), Connectors (46), Domain association (62), Customizable payload (66), Endpoint health monitoring (70), Limits (76); interfaces list (86–90) with the verbatim lead-in "You can create and manage Data Stream using:" and a `[GAP]` where the brief gives no interface links; closing last (94). `## Prerequisites` — N/A, not part of the Overview skeleton. No procedures, steps, or code walkthroughs anywhere on the page, as the kind requires. Decided by `content-types.md`.
- **Closing** — PASS. `## Related resources` (line 94) with four links shaped `[Title](/path/) - reason` (96–99); line 99's reason is a `[GAP]` because the brief gives Real-Time Events only a title and permalink — correct behavior. Links are absolute, `/en/`-prefixed, trailing slash. Decided by `content-types.md` and `style-guide.md` (Links).
- **Heading grammar** — PASS. All headings are sentence-case noun phrases (lines 27, 36, 46, 62, 66, 70, 76, 86, 94); no gerund leads ("monitoring" in line 70 trails and names a thing); no questions; no skipped levels (only `##`); no body H1. Decided by `style-guide.md` (Headings and titles).
- **Step grammar** — N/A. The page contains no procedures, which is what the Overview kind mandates. Colon lead-ins do precede every list (lines 15, 29, 38, 48, 78, 88). Decided by `procedures.md` / `content-types.md`.
- **Frontmatter** — PASS. Five fields present (lines 1–9). Description is 119 characters (measured), opens with the imperative "Feed", is not a banned opener, and does not restate the title. Permalink `/documentation/products/observe/data-stream/` is lowercase ASCII with a trailing slash and no language prefix; namespace `docs_observe_data_stream` is snake_case. Both match the brief exactly. Decided by `style-guide.md` (Frontmatter).
- **Voice sweep** — PASS. Grep sweeps run on the file (observed): zero contractions; zero "we"; zero hits for leverage, seamless, dive into, delve, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., utilize, in order to, empower, plethora, robust, powerful, comprehensive, various; zero "click"; zero "Learn more"; zero time-bound phrasing (currently, recently, now available, new-as-modifier). "Azion recommends" (line 68), not "we recommend". Decided by `style-guide.md`.
- **Components** — PASS. Only `LinkButton` is used, imported from `azion-webkit/linkbutton` directly after the frontmatter (line 11), with live props `label` and `link`. No `<Tabs>`, `<Tag>`, asides, or dead components. `---` separators at lines 25, 74, 92 — three, none immediately after the frontmatter. Decided by `components.md`.
- **Sentence caps** — PASS. Five longest sentences, hand-counted: line 13 opening (22 words), line 68 "Azion recommends NDJSON..." (16), line 72 "The monitoring returns..." (16), line 64 "You can associate domains..." (15), line 64 "Sampling is limited..." (15). All inside the 25-word descriptive budget; table cells exempt. Decided by `simplified-technical-english.md`.
- **Size** — PASS. Body 3,176 characters (measured; target 8,000, cap 16,000); largest section is the definition block at 609 characters (target 2,000, cap 4,000). Every section names its subject and reads alone. Decided by `page-size.md`.
- **Facts** — PASS. Every value traced to the brief: the definition and ASCII encoding (line 13–14 ↔ brief "Definition material"), the three capability bullets, the four data sources and mandatory selection (29–34), the five templates (38–44), all eleven connectors in the brief's order (50–60), Filter Domains / All Domains / Sampling (%) and its two restrictions (64), Max Size in bytes from 1000000, Log Line Separator, Payload Format, NDJSON / `\n` / `$dataset` (68), once-a-minute monitoring, HTTP 504/503, 20-second timeout (72), the three limits in the table (80–84), and the four link permalinks (96–99). Nothing on the page is outside the brief. The three `[GAP]` markers (23, 90, 99) each cover a fact the brief lacks — correct behavior, not findings.

## Verdict

**CONFORMANT.** All eleven checklist items pass or are N/A (step grammar — no procedures on an Overview). No invented facts; the three gaps are correctly marked. The only observations worth recording are sub-finding: the capability definition runs 19 words against the formula's "roughly 10 to 15", and two Related-resources reasons (lines 97–98) restate their link titles rather than adding information.
