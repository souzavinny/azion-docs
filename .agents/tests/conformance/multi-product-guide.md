# Conformance: multi-product-guide

Page: `out/multi-product-guide.mdx` · Brief: `.agents/tests/briefs/multi-product-guide.md` · Kind: **Multi-product guide** (base form how-to, procedural register, 20-word cap)

## Checklist

- **Opening move** — PASS. Lines 13: the problem is stated first in two sentences ("Each route of a storefront needs a different rule. The catalogue can answer from cache, the cart must not, and the checkout needs a rate limit."), then the products enter by role with the formula sentence verbatim: "You configure caching with **Applications** and request filtering with **Firewall**." Matches `content-types.md` (Multi-product guide, opening move). Title (line 2) is the goal in plain language with no product names, as the kind requires.
- **Section order** — PASS. `## Prerequisites` (line 15) as an H2, then one `##` per workflow stage in workflow order: Turn on Application Accelerator (20), Cache the catalogue (35), Bypass cache for the cart (63), Rate-limit the checkout (85), closing `## Next steps` (103). Each stage names its product on entry (lines 22, 37, 65, 87) and stands alone. Ordered by workflow, not by product catalog. `---` separates the stages (lines 33, 61, 83). Decided by `content-types.md`.
- **Closing** — PASS. `## Next steps` (line 103) with four bulleted links shaped `[Title](/en/.../) - reason.` (lines 105–108). All four permalinks and titles come from the brief's link table, with the `/en/` language prefix and trailing slash per `style-guide.md`. Cross-linking matrix satisfied with the links the brief supplies: the caching how-to plus each product's reference.
- **Heading grammar** — PASS. All headings sentence case; task headings imperative (Turn on, Cache, Bypass, Rate-limit); Prerequisites and Next steps as conventional noun phrases; no gerund leads, no questions, no body H1, no skipped levels (page is `##` only). Decided by `style-guide.md`.
- **Step grammar** — PASS. Canonical Console first step on every procedure that opens Console work: lines 26, 41, 69, 91 all read `Access [Azion Console](https://console.azion.com/) > **<Product>** > ...` per `procedures.md`. One action per step throughout; line 94 bundles variable, operator, and argument of a single criterion, which is one gesture under the "small movements" allowance. Location before action (lines 27, 42, 44, 45, 52, 70, 94, 95, 97). Bold labels, italic values (lines 44, 87, 96, 98). UI verbs only: access, go to, turn on, select, enter, set — "set" is `procedures.md`'s own exemplar verb; no click/hit/tap/enable-as-action. A colon lead-in directly before all five procedures (lines 24, 39, 50, 67, 89). An outcome sentence after every procedure (lines 31, 48, 59, 77, 101); lines 48 and 59 state entailed state changes, which `procedures.md` allows, and line 101's `429 Too Many Requests` is a brief fact, not invented output. No optional steps, so the `(Optional)` prefix rule does not arise. Judgment call, noted: the second procedure inside "Cache the catalogue" (line 52) does not repeat the canonical access step. It is a continuation inside the same stage and Console context — the section's first procedure (line 41) carries the canonical step, the brief itself sequences it as "Then in the **Rules Engine** tab...", and `procedures.md`'s first principle is to consolidate access and navigation. Ruled conforming; it is not a "log in"/"in the side menu" opening of the kind `reviewing-docs.md` flags.
- **Frontmatter** — PASS. Five fields (lines 2–10). Description is 111 characters (measured), opens with the imperative "Cache", no banned opener, does not restate the title word for word. Permalink lowercase ASCII, hyphens and slashes only, trailing slash, no language prefix, identical to the brief. Namespace snake_case, identical to the brief. Decided by `style-guide.md`.
- **Voice sweep** — PASS. Grep sweeps run on the file: zero contractions; zero "we"; zero hits for leverage, seamless, dive into, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., utilize, in order to, note that, empower, robust, powerful, comprehensive; zero "click"; zero "Learn more"; zero time-bound phrasing (currently, now available, new, recently). Link phrasing is standard throughout. "is enabled" (line 31) describes state in prose, which `style-guide.md` explicitly permits.
- **Components** — PASS. One `:::note` (lines 79–81), a valid aside type, one per section, placed after the procedure's outcome rather than interrupting the steps. No `<Tabs>`, `<Fragment>`, `<Code>`, or other components, so their mechanics do not arise. `${uri}` appears only inside inline code, which parses safely. Decided by `components.md` via `reviewing-docs.md`.
- **Sentence caps** — PASS. Five longest prose sentences hand-counted against the 20-word procedural budget: line 13 first sentence pair (17 and 10 words), line 37 (18), line 65 (18), line 94 (15), line 101 (18). Nothing over 20. No compound tenses, no `-ing` as a verb or trailing participle ("caching", "request filtering", "starting point" are nouns), no noun cluster over three words counting product and UI names as one unit, no paragraph over six sentences. Decided by `simplified-technical-english.md`.
- **Size** — PASS. Body 4,198 characters (target 8,000, cap 16,000); largest section 1,129 characters (target 2,000, cap 4,000). Measured. Decided by `page-size.md`.
- **Facts** — PASS. Every value traced to the brief: the Console paths, tab and section names, Override Cache Settings, Maximum TTL `3600` with the "changes hourly" reason, `${uri}` semantics, `/catalog`, `/cart`, `/checkout` criteria, Set Cache Policy, Bypass Cache and its three constraints, the 200/5/10 limits in the note, Request URI + starts with, Set Rate Limit, Req/s and Req/min, Average Rate Limit `10`, Client IP address and Global, Maximum burst size (Req/s only), `429 Too Many Requests`, the "starting point, adjust from observed traffic" framing, and all four closing links. The two outcome sentences not verbatim in the brief (lines 48, 59) are entailed state changes from brief facts, which `procedures.md` sanctions. The prerequisites (an application, a firewall) restate the brief's stated route, not new product facts. No `[GAP]` markers, and none were needed — no required fact was missing from the brief. Nothing invented.

## Verdict

**CONFORMANT.** All eleven checklist items pass; none were N/A beyond the sub-rules that do not arise on this page (no code blocks, no optional steps, no tabs). The literal sweeps (contractions, "we", banned words, "click", "Learn more") were run as greps and returned zero hits. One judgment call recorded under step grammar: the in-section continuation procedure at line 52 opens without repeating the canonical Console access step; ruled conforming because access is consolidated at the stage's first procedure and the brief sequences the stage as one flow. If the rule set intends the canonical string on every numbered procedure without exception, `procedures.md` should say so explicitly — that is a rule-clarity note, not an output defect.
