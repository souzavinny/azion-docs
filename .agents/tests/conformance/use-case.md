# Conformance: use-case

**Page:** `out/use-case.mdx` — kind: Use case (pattern of the how-to kind, procedural register)
**Brief:** `.agents/tests/briefs/use-case.md`
**Verdict:** CONFORMANT. Every checklist item passes; three judgment calls are noted inline. One `[GAP]` marker is present and correct.
**Rules changed because of this kind:** none required by this review.

## The checklist every kind runs

- **Opening move** — PASS. The use-case formula (`writing-a-use-case/references/page-shape.md`) replaces the bare how-to scope sentence with a scenario paragraph of three to five sentences plus a not-covered line. The scenario paragraph (line 13) has exactly 5 sentences, counted: what the team has (sentence 1), the constraints (sentences 2–4), and what the page sets up in platform terms, naming **Applications** and **Firewall** (sentence 5). No heading before it; body starts below the `title`-rendered H1. The not-covered line (line 15) sits directly under the scenario and names both exclusions the brief gives: payment processing and inventory synchronization. Not "In this guide".
- **Section order** — PASS. Matches the skeleton in `page-shape.md` exactly: scenario, not-covered line, `## Prerequisites` (line 17, an H2), `---`, `## What you build` (24), `---`, `## Architecture` (34), `---`, three `## Configure ...` sections (45, 72, 93 — at most four allowed), `---`, `## Verify the setup` (112), `---`, `## Next steps` (126). Separator placement matches the skeleton: none between Configure sections, one before Verify and one before Next steps.
- **Closing** — PASS. `## Next steps` per the closings matrix in `content-types.md`; page-shape allows two or three links and the page has three (lines 128–130), each shaped `[Title](/path/) - reason`, all three titles and permalinks from the brief's link table, `/en`-prefixed with trailing slashes. Judgment call, noted: the reasons are lowercase noun phrases ("the reference for the rules you created in the application.") rather than verb-led sentences. The same form appears in this suite's previously passed outputs (architecture, concept, troubleshooting closings), so it is the corpus's accepted reading of "one sentence on why"; the reasons are parallel down the list and each says why the reader would follow it.
- **Heading grammar** — PASS. All headings sentence case; imperative for the three tasks (`Configure the catalogue cache`, `Configure the cart bypass`, `Configure the checkout rate limit`) and for `Verify the setup`; noun phrases for `Prerequisites`, `What you build`, `Architecture`, `Next steps`, all verbatim from the skeleton. No gerund leads, no questions, no body H1 (grep verified), no skipped levels — every body heading is an H2.
- **Step grammar** — PASS. Canonical Console first step on the procedures that open Console work (lines 51, 78, 99): `Access [Azion Console](https://console.azion.com/) > **<Product>** > ...` per `procedures.md`, filled with **Applications** and **Firewall**. Location before action (lines 52, 54, 62, 79, 81, 100). UI verbs only: access, select, enter, set, turn on — "set" is `procedures.md`'s own exemplar; zero click/hit/tap/enable-as-action (grep verified). Bold labels, italic values (`_Override Cache Settings_`, `_Req/s_`, `_starts with_`); typed values in monospace (`3600`, `10`, `/checkout`). A colon lead-in directly before every procedure and the code block (lines 49, 60, 76, 97, 114). An outcome sentence after every procedure (lines 58, 68, 87, 108); lines 58 and 87 are entailed state changes, which `procedures.md` allows, and the `429 Too Many Requests` outcome (line 108) is a brief fact, not invented output. No optional steps, so the `(Optional)` rule does not arise. Two judgment calls, both consistent with the ruling already recorded for the multi-product guide: (1) the second procedure inside `## Configure the catalogue cache` (line 62) does not repeat the canonical access step — it is a continuation in the same section and Console context, and the brief itself sequences it as "Then **Rules Engine** tab →"; (2) step 4 (line 65) bundles "select **Set Cache Policy** and select the cache setting" — one form row, one gesture, matching the brief's own grouping ("**Set Cache Policy** with the cache setting"). Both ruled conforming.
- **Frontmatter** — PASS. Exactly five fields: `title`, `description`, `meta_tags`, `namespace`, `permalink`; no `type`, no unknown keys. Description is 114 characters (measured), inside 50–160, opens with the imperative "Configure", no banned opener, does not restate the title word for word. Permalink `/documentation/products/guides/e-commerce-storefront/` is lowercase ASCII with a trailing slash and no language prefix, identical to the brief. Namespace `docs_guides_ecommerce_storefront` is snake_case, identical to the brief. The title is an imperative verb phrase per the how-to base ("Serve an e-commerce storefront with caching and rate limiting"), which the brief explicitly permits in place of its working title.
- **Voice sweep** — PASS, grep verified: zero contractions; zero "we"/"our"/"us"; zero hits for leverage, seamless, dive into, delve, straightforward, it is important to note, note that, in order to, utilize, facilitate, various, empower, plethora, or any forbidden expression from `terminology.md`; zero "please", "simply", "just", "e.g.", "i.e.", "etc."; zero "click"; zero "Learn more"/"read more"/"click here"; link phrasing uses only the standard formula ("For more information, refer to ..."); no time-bound phrasing ("now" appears only inside outcome sentences stating what the reader now has, which is `procedures.md`'s own formula). Product names are current: Applications, Firewall — no legacy names.
- **Components** — PASS. One import, `Code` from `~/components/Code/Code.astro`, placed directly after the frontmatter and used (line 116) for the copyable `curl` command. One `:::note` aside (lines 89–91), a valid type, one per section. No `<Tabs>` (the brief gives Console as the only interface, so none is required), no dead-list component, no bare `<br>`, no `---` immediately after the frontmatter. The `<Code>` template literal contains no backtick or `${`; the `${uri}` criteria strings sit in inline code, where they are safe.
- **Sentence caps** — PASS. Procedural budget, 20 words. The five longest prose sentences, counted by script: 20, 20, 19, 19, 18 — at the cap twice, over it never.
- **Size** — PASS. Body 5,624 characters (measured, imports excluded) against the 8,000 target and the page-shape translation-aware aim of 7,000. Every `##` section is under the 2,000 target (largest: `## Configure the catalogue cache` at 1,080). Against page-shape's per-section budgets: scenario ≈410 of 500, What you build 650 of 900, Architecture 503 of 700, Configure sections 1,080/927/733 of 1,200, Verify 630 of 700 — all inside; `## Next steps` runs 428 characters against its 200-character budget-table target, noted, and remains far inside every cap in `page-size.md`, which is what the checklist binds to.
- **Facts** — PASS. Every value traced to the brief: the Console paths and tab names, **+ Cache Setting**, **Cache Expiration Policies**, **CDN Cache Settings** _Override Cache Settings_, **Maximum TTL** `3600` with the stated "changes hourly, so one hour is the longest safe TTL" reason, `if ${uri} starts with /catalog` and `/cart`, **Set Cache Policy**, **Bypass Cache** and its origin-not-browser constraint, the **Application Accelerator** module requirement and toggle path, **Request URI** _starts with_ `/checkout`, **Set Rate Limit**, _Req/s_, **Average Rate Limit** `10` with the "starting point, adjust from observed traffic" framing, **Client IP address**, `429 Too Many Requests`, the `curl -I` command verbatim, `X-Cache-Key` and its generated-when-cached explanation, and all seven page links. The one fact the brief withholds — a signal identifying a bypassed cart request — is a `[GAP]` marker (line 120), exactly as the brief orders. Derived-not-invented, noted: the two prerequisites (an application, a firewall) restate what the brief's Console routes require, and "under the same domain" is entailed by one application, one firewall, and one `<your-domain>` placeholder carrying all three paths. No new product value, field, or command anywhere.

## The use-case skill's own checks

- **Scenario sentence budget** — PASS. Five sentences, the allowed maximum; the last one names what the page sets up, in platform terms.
- **Section budgets** — PASS with one note, detailed under Size above.
- **Skeleton** — PASS, detailed under Section order above.
- **Every section stands alone** — PASS. Each Configure section names its setup on entry ("In this storefront setup, ..." / "The storefront catalogue changes hourly, ..."); no "as described above", no cross-section pronoun. The only backward reference ("the cache setting you created", line 65) resolves inside its own section.
- **Inline the specific, link the generic** — PASS. The three rules and their values are inline; cache settings at large, Rules Engine, and Application Accelerator are links.

## commercial-framing.md checklist, item by item

- Scenario names constraints, not market context — PASS. Every sentence states a fact that changes the configuration; no brochure sentence survives the delete test.
- One line says what the use case does not cover — PASS (line 15).
- Every business term translated to a technical need before a product — PASS. The `## What you build` table carries reader words left, platform words middle, consistent phrasing down both columns.
- Every product from the evidence table, not memory — PASS. Applications and Firewall only, both in the brief's evidence table.
- No example figure outside the scenario paragraph — PASS. The scenario carries no figures at all; `3600` and `10` are configure-section values, each explained in its own section, which the Numbers rule requires for values the reader sets.
- No compliance claim — PASS. None made; the brief names no certification, so no link was owed.
- No percentage, price, customer, platform comparison, or industry statistic — PASS, grep and read-through.
- No adjective makes a quality claim about an Azion product — PASS. "Resists abusive traffic" describes behavior tied to a check.
- Every check in `## Verify the setup` tests an outcome, not a step — PASS. The header check and the `429` check test behavior; the cart check is honestly declared unverifiable with a `[GAP]` rather than a rule-appears-in-the-list step check.

## Notes and residual risk

- The closing-link reasons are noun phrases, accepted here on corpus precedent; if the maintainers want verb-led sentences only, the "one sentence on why" formula in `content-types.md` and `page-shape.md` should say so explicitly, since both readings currently pass.
- Two descriptive sentences use platform-actor passives ("must never be shared between users", "content is cached at the edge"); both mirror the brief's own phrasing and fall under the narrow allowance in `simplified-technical-english.md`.
- The Architecture lead-in says "three request paths" above a four-item list; items 3 and 4 both describe the `/checkout` path, so the count refers to paths, not items. Not a defect, recorded to preempt a recount.
