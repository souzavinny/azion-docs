# Conformance: changelog — Bot Manager 1.3.0 and Bot Manager Lite 0.2.0

Page under review: `out/changelog.mdx` (16 lines, 1,087 characters). Kind: Changelog entry (base form Record, descriptive register, 25-word cap). Deciding rules: `skills/contributing/references/content-types.md` (Changelog entry), `skills/contributing/references/changelog.md`, `references/style-guide.md`, `references/procedures.md`, `references/simplified-technical-english.md`, applied per `skills/contributing/references/reviewing-docs.md`. Brief: `.agents/tests/briefs/changelog.md`.

## Checklist

- **Opening move** — PASS. Line 7: `**Bot Manager** now has new versions for both plans: ...` matches the entry formula `**<Product>** now <verb>s <capability>.` — product bolded as subject, present tense, no "we" (content-types.md, Changelog; changelog.md, "The opening sentence carries the whole change").
- **Section order** — PASS. Structure is `## July 10, 2026` (line 1) > `### Marketplace` (line 3) > `#### Bot Manager 1.3.0 and Bot Manager Lite 0.2.0` (line 5); the date, Marketplace section, and heading are all mandated verbatim by the brief (brief lines 9–10). Entry body order matches changelog.md's shape: opening sentence (line 7), what it means concretely (line 9), who is not affected plus how to obtain (line 11), code-sample slot (line 13, as a `[GAP]`), documentation link (line 15). Three body paragraphs, none with internal headings, inside the three-to-six band (changelog.md, "Entry shape").
- **Closing** — PASS. Line 15 is the exact required sentence: `For more information, refer to [Azion Bot Manager Lite](/en/documentation/products/secure/firewall/bot-manager-lite/).` (changelog.md, "Close with the documentation link"; content-types.md, "Entry closing"). The closings matrix assigns changelogs no `## Next steps`/`## Related resources` section, and none is present. The `[Title](/path/) - reason` link shape applies to closing sections, which this kind does not have — no violation.
- **Heading grammar** — PASS. No body H1; levels run ## > ### > #### with none skipped; all headings are noun phrases in sentence case with product-name capitals preserved; no gerund leads; no questions (style-guide.md, "Headings and titles"). The entry heading `Bot Manager 1.3.0 and Bot Manager Lite 0.2.0` (line 5) is supplied verbatim by the brief as the entry identity (brief line 9); changelog.md's "titles are declarative claims" preference cannot override a given heading without inventing wording the brief did not supply.
- **Step grammar** — N/A. The page contains no numbered procedure, no Console walkthrough, and no code block; procedures.md has nothing to bind to. The colon lead-in rule for the code sample is moot because the sample itself is a `[GAP]` marker (line 13).
- **Frontmatter** — N/A. The brief orders an `.mdx` fragment with no page frontmatter (brief line 10); the README names this exact case as N/A.
- **Voice sweep** — PASS. Grep sweeps run on the file, all zero matches: contractions (don't, it's, can't, you'll, won't, isn't, doesn't, we're, let's, there's, and any `'`-contraction pattern); the word "we"; banned/filler/marketing terms (leverage, seamless, dive into, straightforward, it is important to note, please, simply, just, e.g., i.e., etc., delve, utilize, in order to, note that, empower, plethora, robust, powerful, comprehensive, cutting-edge, state-of-the-art, facilitate); "click"; "Learn more". Link phrasing: line 15 uses the standard formula; lines 11's inline links name their destinations (style-guide.md, "Links"). Time-bound phrasing ("now", "new", the date, version numbers, "formerly named") is the content of a changelog and exempt here (style-guide.md, "Documentation reads as timeless"; changelog.md, "What changelogs are exempt from"). The one passive, "was formerly named Advanced" (line 7), has the platform as actor — allowed in descriptive register (simplified-technical-english.md, "Active voice").
- **Components** — N/A. No components, no asides, no `<Tabs>`, no `<Fragment>`, no `<Code>` on the page; nothing from the dead list is imported. The `<Code>` block changelog.md would require for the configuration change is correctly represented by the `[GAP]` marker (line 13) because the brief supplies no sample.
- **Sentence caps** — PASS. Descriptive budget, 25 words. The five longest sentences, counted: line 7 opening, 21 words; line 11 "The standard version ... Service Delivery team", 18 words; line 11 "To get the new Lite version ...", 13 words; line 9 "The `good_fingerprint_list` argument ...", 11 words; line 9 "The release adds nine new static rules and two new arguments", 11 words. All under 25. No compound tenses, no `-ing` used as a verb or trailing participle, no noun cluster over three words (product names count as one unit), no dropped articles, no paragraph over six sentences (simplified-technical-english.md, "The rules").
- **Size** — PASS. 1,087 characters total, far inside every cap in page-size.md — and changelogs are exempt from size caps regardless (changelog.md, "What changelogs are exempt from").
- **Facts** — PASS. Every value traces to the brief: the date, section, and heading (brief line 9); both version numbers and the plan rename (brief line 14); nine static rules (brief line 15); the `good_fingerprint_list` and `block_ai_bots` behaviors, near-verbatim (brief lines 16–17); the Marketplace launch path and Console URL (brief line 18); on-demand availability via the Service Delivery team (brief line 19); the no-upgrade blast radius, stated without inventing an auto-update behavior (brief line 20, satisfying changelog.md's "State the blast radius"); both documentation links and their texts (brief lines 24–27), with the `/en/` prefix added per style-guide.md's link rule — composition, not invention. "Two new arguments" (line 9) is a count of the two given arguments, derived, not invented. The missing code sample is a `[GAP: ...]` marker (line 13), which is the mandated behavior for a fact the brief lacks — PASS, not a finding.

## Verdict

**CONFORMANT.** 9 PASS, 3 N/A (step grammar, frontmatter, components — none can apply to a changelog fragment with no procedures, no frontmatter, and no components), 0 FAIL. The single `[GAP]` marker is correct generator behavior under the brief's standing rule.
