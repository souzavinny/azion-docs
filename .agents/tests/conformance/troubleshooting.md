# Conformance: troubleshooting — `out/troubleshooting.mdx`

Reviewed against the checklist in `.agents/tests/README.md`, the Troubleshooting entry in `skills/contributing/references/content-types.md`, `references/style-guide.md`, `references/procedures.md`, `references/simplified-technical-english.md`, `references/components.md`, `references/page-size.md`, and `skills/contributing/references/reviewing-docs.md`. Kind judged: Troubleshooting (How-to base form, procedural register, 20-word cap).

## Checklist

- **Opening move** — PASS. Line 14 is a scope sentence naming the product (**Web Application Firewall (WAF)**) and the symptom class (legitimate requests blocked / false positives), per `content-types.md` (Troubleshooting). The kind's formula is not verbatim, so no verbatim string is required. The second intro paragraph (line 16, Real-Time Events diagnostics) carries brief facts with no home in either symptom section; no rule forbids intro material before the first symptom.
- **Section order** — PASS. One `##` per symptom (lines 20, 47), each internally ordered symptom → cause → fix → outcome per `content-types.md`: symptom at 22/49, cause at 24–30/51–58, fix at 32–41/60–97, outcome at 43 and 76/93. `## Prerequisites` is not required by this kind. Each section is self-contained: the WAF is named inside every section, no back-references (`page-size.md`).
- **Closing** — PASS. `## Related resources` (line 101), the closing this kind requires per the matrix in `content-types.md`. All four links shaped `[Title](/path/) - reason` (lines 103–106), targets are the how-to and reference pages the fixes lean on, and all four permalinks match the brief's Links table with the `/en` prefix and trailing slash per `style-guide.md`.
- **Heading grammar** — PASS. `` ## `403 Forbidden` on legitimate requests `` (line 20) is the noun-phrase-with-verbatim-error-string form `content-types.md` mandates; `## Blocked form posts or uploads` (line 47) is a noun phrase (participle adjective, not a gerund lead); sentence case throughout; no questions, no skipped levels, no body H1 (`style-guide.md`).
- **Step grammar** — PASS. Both Console procedures open with the canonical string `Access [Azion Console](https://console.azion.com/) > **WAF Rules** > **your rule set**.` (lines 34, 70) per `procedures.md`. One action per step; location before action (lines 37, 70–72); purpose before action (lines 39, 74); `(Optional)` prefix (line 39); UI verbs only — select, go to, enter-class verbs, no click/enable; bold labels, italic values (lines 36, 38, 73); outcome sentence after every procedure (lines 43, 76, 93); colon lead-in before every procedure and code block (lines 32, 68, 82). The API path is correctly a lead-in plus `<Code>`, not a numbered list.
- **Frontmatter** — PASS. Five fields (lines 2–8). Description is 103 characters, opens with the imperative "Identify", no banned opener, does not restate the title (`style-guide.md` description contract). Permalink `/documentation/products/secure/troubleshoot-waf-false-positives/` is lowercase ASCII, trailing slash, no language prefix, and matches the brief. Namespace `docs_secure_troubleshoot_waf_false_positives` is snake_case and matches the brief.
- **Voice sweep** — PASS. Grep swept: zero contractions; zero "we"; zero hits for leverage, seamless, dive into, delve, straightforward, it is important to note, note that, please, simply, just, e.g., i.e., etc., in order to, utilize, facilitate, various, empower, plethora; zero "click"; zero "Learn more"; no non-standard link phrasing; no time-bound phrasing ("the last 168 hours" at line 16 is a rolling retention window, a product fact from the brief; "new" at line 43 modifies the just-created rule inside an outcome sentence and is the brief's own wording, not timeline phrasing — `style-guide.md`).
- **Components** — PASS. Only live components; imports directly after frontmatter (lines 11–12) with the exact paths in `components.md`; `<Tabs client:visible>` (line 62); every `tab.x` has its `panel.x`, Console panel first, canonical keys; blank lines around markdown inside every `<Fragment>` (lines 66–78, 80–95); `<Code lang="json">` for copyable input, `json` in the allowed tag set; template-literal escaping verified by execution — `\\\\s` renders `\\s`, matching the brief's JSON exactly, and the rendered block parses as JSON. No asides, no bare `<br>`, no `---` immediately after the frontmatter (three `---` separators at lines 18, 45, 99).
- **Sentence caps** — PASS. The five longest prose sentences measure 18 (line 73), 17 (line 16), 16 (line 49), 16 (line 22), and 15 (line 74) words — all inside the 20-word procedural budget of `simplified-technical-english.md`. No compound tenses, no `-ing` as a verb, no trailing participle clauses, no noun cluster over three words, no dropped articles.
- **Size** — PASS. Body 4,141 characters (target 8,000); sections: intro 533, `403 Forbidden` 1,215, form posts (including the `<Tabs>` block) 1,799, Related resources 591 — all inside the 2,000-character section target of `page-size.md`.
- **Facts** — PASS. Every value traced to the brief: rule IDs 1000/1013/1302/2/13 and their matches; 128 kilobytes; Learning-mode note; the 3-day Tuning window; 168-hour Real-Time Events retention; `Waf Block`/`Waf Score`/`Waf Ev Headers`; every UI label and Match Zone option; the v4 exceptions endpoint and full JSON body; all four link permalinks. `<your-reason>` is the house placeholder form for the brief's elided `"reason": "..."` value (`style-guide.md`). The API outcome (line 93) is an entailed state change, allowed by `procedures.md`, restating the brief's "matching requests stop being blocked". One `[GAP: name of the control that saves the allowed rule]` at line 74 where the brief omits the save control — correct behavior, PASS.

## Verdict

**CONFORMANT.** All eleven checklist items pass with no manual edits. The single `[GAP]` marker is the mandated response to a fact the brief does not carry.

Judgment calls examined and held: the two-sentence opening (scope sentence present; formula not verbatim); "Use the **Field** checkbox" at line 40 ("use" is not on the banned-verb list in `style-guide.md`, and the checkbox is named before the action); "Fill in" at line 73 (the brief's own wording, not a banned verb); restating the 8-step Tuning procedure (the brief supplies it as the fix, the kind allows "a numbered procedure", and Related resources links the complete guide it summarizes).
